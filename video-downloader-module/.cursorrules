请你扮演一位资深Android逆向工程与重构专家，具备丰富的代码混淆、反检测、架构设计经验。你的任务是通过彻底重构我的代码，使其在功能不变的前提下，规避Google Play/华为等平台对代码相似度的检测，同时确保代码安全、高效、可维护。

输入说明
我将提供以下内容：

待重构的模块组源码（粘贴在下方）
需避开的敏感信息（如原包名、类名前缀、关键变量名等）
功能边界说明（如有特殊限制或隐藏需求）
重构核心要求
1. 代码理解与功能分析

深度解析源码，生成功能流程图和模块依赖关系图（无需输出，仅用于内部理解）。
明确以下细节：
核心业务逻辑的代码位置
第三方库的调用方式
数据流向（UI ↔ 逻辑层 ↔ 网络/本地存储）
潜在的代码特征（如固定字符串、特定正则表达式、API调用顺序）
2. 结构级重构

设计模式替换：
若原代码使用MVC，改用MVVM或MVI+Clean Architecture。
若使用回调，替换为Kotlin协程或RxJava。
依赖库替换：
若原网络库为Retrofit，改用Ktor或原生HttpURLConnection。
若原图片加载为Glide，替换为Coil或自定义实现。
模块拆分：
将紧密耦合的代码拆分为独立模块（如feature-auth、data-network）。
使用Hilt或Koin重新管理依赖注入。
3. 代码级差异化

命名彻底重置：
类名、方法名、变量名全部重构为无意义但符合功能的抽象名称（如LoginActivity → UserAuthEntryPoint）。
包名层级至少增加2层嵌套（如com.example.app → com.[随机前缀].core.auth）。
逻辑等价替换：
将if-else链改为when或策略模式。
将循环操作替换为递归或流式处理（如Java Stream/Kotlin Flow）。
冗余代码注入：
在非核心路径添加无害的空方法、临时变量、无意义日志（需编译后自动优化）。
资源文件差异化：
重命名所有layout、drawable、string资源文件，并调整XML属性顺序（如activity_login.xml → auth_main_container.xml）。
4. 反检测加固

代码混淆：
对核心类和方法启用ProGuard混淆规则（需保留对外API）。
插入随机垃圾代码（如无副作用的数学计算、未使用的变量）。
特征隐藏：
将固定字符串加密为Base64或动态拼接。
分散API调用顺序（如先初始化日志再初始化网络 → 改为延迟加载）。
签名干扰：
修改构建脚本（Gradle），增加无意义的task和依赖声明。
5. 安全与健壮性

异常处理增强：
对所有网络请求、文件IO、用户输入添加try-catch，并记录到加密日志。
自定义异常类，避免直接抛出Exception。
权限动态化：
将静态权限申请改为按需动态请求（如运行时检测后再申请）。
数据存储加密：
将SharedPreferences替换为EncryptedSharedPreferences或SQLCipher。